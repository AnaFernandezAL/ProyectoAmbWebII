@model X.PagedList.IPagedList<proyecto.Application.DTOs.SubastaDTO>
@using X.PagedList.Mvc.Core

@{
    ViewData["Title"] = "Subastas";
}

<div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-2 mb-3">
    <div>
        <h1 class="h3 mb-1">Subastas</h1>
        <p class="text-muted mb-0">Listado general de subastas registradas en el sistema.</p>
    </div>

    <div class="d-flex align-items-center gap-2">
        <span class="badge text-bg-secondary" title="Total de subastas">@Model?.TotalItemCount</span>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg me-1"></i> Nueva subasta
        </a>
    </div>
</div>

<div class="card shadow-sm">
    <ul class="nav nav-tabs card-header-tabs px-3">
        <li class="nav-item">
            <a class="nav-link @(ViewData["Title"]?.ToString() == "Subastas" ? "active" : "")" asp-action="Index">
                <i class="bi bi-list-ul me-1"></i> Todas
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(ViewData["Title"]?.ToString() == "Subastas Activas" ? "active" : "")" asp-action="Activas">
                <i class="bi bi-play-circle me-1"></i> Activas
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link @(ViewData["Title"]?.ToString() == "Subastas Finalizadas" ? "active" : "")" asp-action="Finalizadas">
                <i class="bi bi-stop-circle me-1"></i> Finalizadas
            </a>
        </li>
    </ul>

    <div class="card-body p-0">
        @if (Model == null || !Model.Any())
        {
            <div class="p-4 text-center">
                <div class="display-6 mb-2">
                    <i class="bi bi-box-seam"></i>
                </div>
                <h2 class="h5">No hay subastas registradas</h2>
                <p class="text-muted mb-3">Crea la primera subasta para iniciar el sistema.</p>
                <a asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-lg me-1"></i> Crear subasta
                </a>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light position-sticky top-0" style="z-index:1;">
                        <tr>
                            <th scope="col" class="ps-3">Objeto</th>
                            <th scope="col">Imagen</th>
                            <th scope="col">Fecha Inicio</th>
                            <th scope="col">Fecha Cierre</th>
                            <th scope="col">Cantidad de Pujas</th>
                            <th scope="col" class="text-end pe-3" style="width: 220px;">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var subasta in Model)
                        {
                            <tr>
                                <td class="ps-3">
                                    <div class="fw-semibold">@subasta.Carta?.NombreCarta</div>
                                </td>
                                <td>
                                    @if (subasta.Carta?.ImagenesCartaNavigation != null && subasta.Carta.ImagenesCartaNavigation.Any())
                                    {
                                        <img src="@subasta.Carta.ImagenesCartaNavigation.First().UrlImagen"
                                             alt="@subasta.Carta.NombreCarta"
                                             class="img-thumbnail"
                                             style="max-height:80px;" />
                                    }

                                    else
                                    {
                                        <span class="text-muted">Sin imagen</span>
                                    }
                                </td>
                                <td>@subasta.FechaInicio.ToString("dd/MM/yyyy")</td>
                                <td>@subasta.FechaCierre.ToString("dd/MM/yyyy")</td>
                                <td>
                                    <span class="badge bg-info text-dark">@subasta.CantidadPujas</span>
                                </td>
                                <td class="text-end pe-3">
                                    <div class="btn-group" role="group" aria-label="Acciones de la subasta">
                                        <a asp-action="Details" asp-route-id="@subasta.SubastaId"
                                           class="btn btn-outline-secondary btn-sm"
                                           title="Ver detalle">
                                            <i class="bi bi-eye"></i>
                                            <span class="d-none d-lg-inline ms-1">Detalle</span>
                                        </a>

                                        <a asp-action="Edit" asp-route-id="@subasta.SubastaId"
                                           class="btn btn-outline-primary btn-sm"
                                           title="Editar">
                                            <i class="bi bi-pencil-square"></i>
                                            <span class="d-none d-lg-inline ms-1">Editar</span>
                                        </a>

                                        <a asp-action="Delete" asp-route-id="@subasta.SubastaId"
                                           class="btn btn-outline-danger btn-sm"
                                           title="Eliminar">
                                            <i class="bi bi-trash"></i>
                                            <span class="d-none d-lg-inline ms-1">Eliminar</span>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>

    <div class="card-footer bg-body d-flex flex-column flex-md-row align-items-center justify-content-between gap-2 small text-muted">
        <div>
            Total: <strong>@Model?.TotalItemCount</strong> subasta(s)
        </div>

        @if (Model != null && Model.PageCount > 1)
        {
            <nav aria-label="Paginación de subastas">
                @Html.PagedListPager(
                Model,
                        page => Url.Action("Index", new { page }),
                        new PagedListRenderOptions
                        {
                            UlElementClasses = new[] { "pagination", "mb-0" },
                            LiElementClasses = new[] { "page-item" },
                            PageClasses = new[] { "page-link" },
                            DisplayLinkToFirstPage = PagedListDisplayMode.Always,
                            DisplayLinkToLastPage = PagedListDisplayMode.Always,
                            DisplayLinkToPreviousPage = PagedListDisplayMode.Always,
                            DisplayLinkToNextPage = PagedListDisplayMode.Always,
                            MaximumPageNumbersToDisplay = 5
                        }
                        )
        </nav>
                }
    </div>
</div>